"""Horus — a calibrated red-team harness for LLM applications.

Public API is intentionally small; most users drive the framework through the
CLI (`horus run`) and YAML configs. See README.md.
"""

__version__ = "0.1.0"
